"
A raw information input 

- from text is the simplest.

- I m associated to a context (maanged by SybiosIS)
- I can eventually find the context by myself... (but restrained for now))
"
Class {
	#name : #SymbiosAbstractInput,
	#superclass : #SymbiosAbstractInfo,
	#instVars : [
		'meta',
		'context'
	],
	#category : #Symbios
}
